dish_name, 
material_list(name),
URL, 
URL_title, 
classification, 
flavor, 
snatch,

This is the 1st modification.

I am on the local branch now, it's not the master branch.

Is it better if we use files to be the interface between the controller and the DBM?

another directory...
